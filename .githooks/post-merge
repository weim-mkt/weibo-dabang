#!/usr/bin/env bash
# Git post-merge hook: warn if an upstream merge reintroduced scripts/R/.
# Activate by running once: git config core.hooksPath .githooks

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/.claude/hooks/check-code-path.sh"
